apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-deployment
  labels:
    name: chat-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chat
  template:
    metadata:
      labels:
        app: chat
    spec:
      containers:
        - image: chat-service
          name: chat-main
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: '120m'
              memory: '120M'
            limits:
              cpu: '250m'
              memory: '250M'
          livenessProbe:
            httpGet:
              path: /health
              port: 5007
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 5007
            initialDelaySeconds: 10
            periodSeconds: 5
          env:
            - name: ACCESS_TOKEN_SECRET
              valueFrom:
                secretKeyRef:
                  name: auth
                  key: ACCESS_TOKEN_SECRET
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: database
                  key: AUTH_DB # Fallback / DB cluster URI
            - name: REDIS_URL
              value: 'redis://redis-service:6379'
            - name: PORT
              value: '5007'
            - name: NODE_ENV
              value: 'production'
          ports:
            - containerPort: 5007
              name: http
